@model IEnumerable<CajeroAutomatico.Entities.DispensadorEfectivo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ATM.Direccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ATM.Direccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDispensadorefectivo }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDispensadorefectivo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDispensadorefectivo })
        </td>
    </tr>
}

</table>
