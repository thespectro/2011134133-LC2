@model IEnumerable<CajeroAutomatico.Entities.BaseDatos>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Administrador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Administrador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBaseDatos }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBaseDatos }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBaseDatos })
        </td>
    </tr>
}

</table>
